# Copy to .env and fill in values

# Path to PyTorch state-dict file (relative to working directory inside container)
MODEL_WEIGHTS_PATH=./weights/model.pth

# Optional: HTTPS URL to download weights on cold start (e.g. public GCS/S3 URL)
MODEL_URL=

# "cpu" or "cuda"
DEVICE=cpu
